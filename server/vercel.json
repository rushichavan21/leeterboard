{
  "version": 2,
  "builds": [
    {
      "src": "api/manual-sync.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/manual-sync", "dest": "api/manual-sync.js" }
  ],
  "crons": [
    {
      "path": "/manual-sync",
      "schedule": "*/15 * * * *"
    }
  ]
}
